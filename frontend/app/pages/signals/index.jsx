/* /signals — Signals list (Fase 4, task 4.9). */
import Shell from "../../components/Shell";
import SignalsScreen from "../../screens/SignalsScreen";

export default function SignalsPage() {
  return (
    <Shell>
      <SignalsScreen />
    </Shell>
  );
}
