/* /external-signals — External signals feed (X, Telegram, RSS). */
import Shell from "../../components/Shell";
import ExternalSignalsScreen from "../../screens/ExternalSignalsScreen";

export default function ExternalSignalsPage() {
  return (
    <Shell>
      <ExternalSignalsScreen />
    </Shell>
  );
}
