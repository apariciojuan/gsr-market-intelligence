# Application
APP_ENV=development

# Real API for data; mock auth (admin / 1234) until POST /auth/jwt/login exists on backend.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_REAL_DOMAINS=resolutions,markets,contracts,search,health,dashboard,signals,externalSignals,ecosystem

# When backend auth is live, switch to:
# NEXT_PUBLIC_DATA_SOURCE=api
