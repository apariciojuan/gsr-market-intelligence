FROM timescale/timescaledb-ha:pg17

# The Timescale HA image ships PostgreSQL 17 with TimescaleDB and pgvector
# installed for both amd64 and arm64. Keeping the extensions in the base image
# avoids pinning PGDG package versions that can disappear from Debian repos.
